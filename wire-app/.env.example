# Same Supabase project as the wire box scanner
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: full URL to the scanner app (defaults to /wire-scanner/ on same host)
# VITE_WIRE_SCANNER_URL=https://your-app.ondigitalocean.app/wire-scanner/
