# SHS Web Apps (home)

Simple navigation landing page for the DigitalOcean root URL.

Lists:

- Reorder → `/reorder/`
- Wire Tracker → `/wire/`
- Wire Box Scanner → `/wire-scanner/`

## Build

```bash
npm run build
```

Output is in `dist/`.
